<h3>Abteilungen</h3>

<table>
  <thead>
    <tr>
      <th>Bezeichnung</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @divisions.each do |division| %>
      <tr>
        <td><%= division.bezeichnung %></td>
        <td><%= link_to 'Anzeigen', division %></td>
        <td><%= link_to 'Ändern', edit_division_path(division) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neue Abteilung', new_division_path %> |
<%= link_to 'Admin Bereich', admin_pages_index_path %>
