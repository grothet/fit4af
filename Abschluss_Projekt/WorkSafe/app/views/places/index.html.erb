<h3>Orte</h3>

<table>
  <thead>
    <tr>
      <th>Kostenstelle</th>
      <th>Bezeichnung</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @places.each do |place| %>
      <tr>
        <td><%= place.kostenstelle %></td>
        <td><%= place.bezeichnung %></td>
        <td><%= link_to 'Anzeigen', place %></td>
        <td><%= link_to 'Ändern', edit_place_path(place) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neuer Ort', new_place_path %> |
<%= link_to 'Admin Bereich', admin_pages_index_path %>

